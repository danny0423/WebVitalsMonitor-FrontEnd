好的!我來為這個專案規劃完整的樣式規格書。

---

# VitalMetrics 樣式規格書 (Style Guide)

## 📋 目錄

1. [設計原則](#設計原則)
2. [色彩系統](#色彩系統)
3. [字體系統](#字體系統)
4. [間距系統](#間距系統)
5. [組件樣式](#組件樣式)
6. [佈局系統](#佈局系統)
7. [響應式設計](#響應式設計)
8. [動畫效果](#動畫效果)
9. [SCSS 架構](#scss-架構)

---

## 🎨 設計原則

### 核心理念
```
簡潔 (Minimal)
├─ 專注於數據呈現
├─ 避免過度裝飾
└─ 留白適當

專業 (Professional)
├─ 配色穩重
├─ 字體清晰
└─ 佈局規整

高效 (Efficient)
├─ 資訊層級分明
├─ 操作路徑短
└─ 載入速度快
```

---

## 🎨 色彩系統

### 主色調 (Primary Colors)

```scss
// src/styles/_variables.scss

// === 品牌色 ===
$primary: #3b82f6;          // 主色 - 藍色
$primary-light: #60a5fa;    // 淺藍
$primary-dark: #2563eb;     // 深藍
$primary-50: #eff6ff;       // 極淺藍 (背景)
$primary-100: #dbeafe;      // 很淺藍 (hover背景)

// === 中性色 ===
$gray-50: #f9fafb;
$gray-100: #f3f4f6;
$gray-200: #e5e7eb;
$gray-300: #d1d5db;
$gray-400: #9ca3af;
$gray-500: #6b7280;
$gray-600: #4b5563;
$gray-700: #374151;
$gray-800: #1f2937;
$gray-900: #111827;

// === 語意色 ===
// Success (Good)
$success: #22c55e;
$success-light: #86efac;
$success-dark: #16a34a;
$success-bg: #f0fdf4;

// Warning (Needs Improvement)
$warning: #f59e0b;
$warning-light: #fbbf24;
$warning-dark: #d97706;
$warning-bg: #fffbeb;

// Error (Poor)
$error: #ef4444;
$error-light: #f87171;
$error-dark: #dc2626;
$error-bg: #fef2f2;

// Info
$info: #3b82f6;
$info-light: #60a5fa;
$info-dark: #2563eb;
$info-bg: #eff6ff;

// === 背景色 ===
$bg-primary: #ffffff;
$bg-secondary: #f9fafb;
$bg-tertiary: #f3f4f6;

// === 文字色 ===
$text-primary: #111827;
$text-secondary: #6b7280;
$text-tertiary: #9ca3af;
$text-inverse: #ffffff;

// === 邊框色 ===
$border-light: #e5e7eb;
$border-medium: #d1d5db;
$border-dark: #9ca3af;

// === Web Vitals 專用色 ===
$lcp-good: #0cce6b;
$lcp-needs-improvement: #ffa400;
$lcp-poor: #ff4e42;

$inp-good: #0cce6b;
$inp-needs-improvement: #ffa400;
$inp-poor: #ff4e42;

$cls-good: #0cce6b;
$cls-needs-improvement: #ffa400;
$cls-poor: #ff4e42;
```

### 色彩使用場景

```scss
// === 使用範例 ===

// 按鈕
.btn-primary {
  background: $primary;
  color: $text-inverse;
  
  &:hover {
    background: $primary-dark;
  }
}

// 指標卡片 - Good
.metric-card.good {
  border-left: 4px solid $success;
  background: $success-bg;
  
  .value {
    color: $success-dark;
  }
}

// 指標卡片 - Poor
.metric-card.poor {
  border-left: 4px solid $error;
  background: $error-bg;
  
  .value {
    color: $error-dark;
  }
}
```

---

## 📝 字體系統

### 字體家族

```scss
// === 字體定義 ===
$font-family-base: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto',
  'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans',
  'Helvetica Neue', sans-serif;

$font-family-mono: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono',
  Consolas, 'Courier New', monospace;

// === 字體大小 ===
$font-size-xs: 0.75rem;    // 12px
$font-size-sm: 0.875rem;   // 14px
$font-size-base: 1rem;     // 16px
$font-size-lg: 1.125rem;   // 18px
$font-size-xl: 1.25rem;    // 20px
$font-size-2xl: 1.5rem;    // 24px
$font-size-3xl: 1.875rem;  // 30px
$font-size-4xl: 2.25rem;   // 36px
$font-size-5xl: 3rem;      // 48px

// === 字體粗細 ===
$font-weight-light: 300;
$font-weight-normal: 400;
$font-weight-medium: 500;
$font-weight-semibold: 600;
$font-weight-bold: 700;

// === 行高 ===
$line-height-tight: 1.25;
$line-height-normal: 1.5;
$line-height-relaxed: 1.75;
$line-height-loose: 2;
```

### 字體使用規範

```scss
// === 標題層級 ===
h1, .h1 {
  font-size: $font-size-4xl;
  font-weight: $font-weight-bold;
  line-height: $line-height-tight;
  color: $text-primary;
  margin-bottom: 1rem;
}

h2, .h2 {
  font-size: $font-size-3xl;
  font-weight: $font-weight-bold;
  line-height: $line-height-tight;
  color: $text-primary;
  margin-bottom: 0.875rem;
}

h3, .h3 {
  font-size: $font-size-2xl;
  font-weight: $font-weight-semibold;
  line-height: $line-height-normal;
  color: $text-primary;
  margin-bottom: 0.75rem;
}

h4, .h4 {
  font-size: $font-size-xl;
  font-weight: $font-weight-semibold;
  line-height: $line-height-normal;
  color: $text-primary;
  margin-bottom: 0.5rem;
}

// === 內文 ===
body {
  font-family: $font-family-base;
  font-size: $font-size-base;
  line-height: $line-height-normal;
  color: $text-primary;
}

.text-sm {
  font-size: $font-size-sm;
}

.text-xs {
  font-size: $font-size-xs;
}

// === 數字顯示 (指標值) ===
.metric-value {
  font-family: $font-family-mono;
  font-size: $font-size-4xl;
  font-weight: $font-weight-bold;
  line-height: 1;
  letter-spacing: -0.02em;
}
```

---

## 📏 間距系統

### 間距變數

```scss
// === 間距尺度 (基於 4px) ===
$spacing-0: 0;
$spacing-1: 0.25rem;   // 4px
$spacing-2: 0.5rem;    // 8px
$spacing-3: 0.75rem;   // 12px
$spacing-4: 1rem;      // 16px
$spacing-5: 1.25rem;   // 20px
$spacing-6: 1.5rem;    // 24px
$spacing-8: 2rem;      // 32px
$spacing-10: 2.5rem;   // 40px
$spacing-12: 3rem;     // 48px
$spacing-16: 4rem;     // 64px
$spacing-20: 5rem;     // 80px

// === 容器寬度 ===
$container-sm: 640px;
$container-md: 768px;
$container-lg: 1024px;
$container-xl: 1280px;
$container-2xl: 1536px;
```

### 間距使用場景

```scss
// === 卡片內距 ===
.card {
  padding: $spacing-6;
  
  &-sm {
    padding: $spacing-4;
  }
  
  &-lg {
    padding: $spacing-8;
  }
}

// === 元素間距 ===
.section {
  margin-bottom: $spacing-8;
  
  &:last-child {
    margin-bottom: 0;
  }
}

// === 網格間距 ===
.grid {
  gap: $spacing-6;
  
  &-sm {
    gap: $spacing-4;
  }
  
  &-lg {
    gap: $spacing-8;
  }
}
```

---

## 🧩 組件樣式

### 按鈕 (Buttons)

```scss
// === 基礎按鈕 ===
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: $spacing-2;
  padding: $spacing-2 $spacing-4;
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  line-height: $line-height-tight;
  border-radius: 6px;
  border: 1px solid transparent;
  cursor: pointer;
  transition: all 0.2s ease;
  
  &:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }
}

// === 按鈕變體 ===
.btn-primary {
  background: $primary;
  color: $text-inverse;
  
  &:hover:not(:disabled) {
    background: $primary-dark;
  }
  
  &:active {
    background: $primary-dark;
    transform: scale(0.98);
  }
}

.btn-secondary {
  background: $gray-100;
  color: $text-primary;
  
  &:hover:not(:disabled) {
    background: $gray-200;
  }
}

.btn-outline {
  background: transparent;
  border-color: $border-medium;
  color: $text-primary;
  
  &:hover:not(:disabled) {
    background: $gray-50;
    border-color: $border-dark;
  }
}

.btn-ghost {
  background: transparent;
  color: $text-secondary;
  
  &:hover:not(:disabled) {
    background: $gray-100;
    color: $text-primary;
  }
}

// === 按鈕尺寸 ===
.btn-sm {
  padding: $spacing-1 $spacing-3;
  font-size: $font-size-xs;
}

.btn-lg {
  padding: $spacing-3 $spacing-6;
  font-size: $font-size-base;
}
```

### 卡片 (Cards)

```scss
// === 基礎卡片 ===
.card {
  background: $bg-primary;
  border-radius: 8px;
  border: 1px solid $border-light;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
  padding: $spacing-6;
  transition: all 0.2s ease;
  
  &:hover {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
  }
}

// === 指標卡片 ===
.metric-card {
  position: relative;
  padding: $spacing-6;
  border-left: 4px solid transparent;
  
  &.good {
    border-left-color: $success;
    background: linear-gradient(to right, $success-bg 0%, $bg-primary 100%);
  }
  
  &.needs-improvement {
    border-left-color: $warning;
    background: linear-gradient(to right, $warning-bg 0%, $bg-primary 100%);
  }
  
  &.poor {
    border-left-color: $error;
    background: linear-gradient(to right, $error-bg 0%, $bg-primary 100%);
  }
}

.metric-card__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: $spacing-4;
}

.metric-card__title {
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  color: $text-secondary;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.metric-card__badge {
  display: inline-block;
  padding: $spacing-1 $spacing-2;
  font-size: $font-size-xs;
  font-weight: $font-weight-semibold;
  border-radius: 4px;
  
  &.good {
    background: rgba($success, 0.1);
    color: $success-dark;
  }
  
  &.needs-improvement {
    background: rgba($warning, 0.1);
    color: $warning-dark;
  }
  
  &.poor {
    background: rgba($error, 0.1);
    color: $error-dark;
  }
}

.metric-card__value {
  display: flex;
  align-items: baseline;
  gap: $spacing-2;
  margin-bottom: $spacing-3;
}

.metric-card__number {
  font-family: $font-family-mono;
  font-size: $font-size-4xl;
  font-weight: $font-weight-bold;
  line-height: 1;
  
  &.good {
    color: $success-dark;
  }
  
  &.needs-improvement {
    color: $warning-dark;
  }
  
  &.poor {
    color: $error-dark;
  }
}

.metric-card__unit {
  font-size: $font-size-base;
  color: $text-tertiary;
}

.metric-card__trend {
  display: flex;
  align-items: center;
  gap: $spacing-1;
  font-size: $font-size-sm;
  
  &.up {
    color: $error;
  }
  
  &.down {
    color: $success;
  }
  
  &.stable {
    color: $text-secondary;
  }
}
```

### 表單元素 (Form Elements)

```scss
// === Input ===
.input {
  width: 100%;
  padding: $spacing-2 $spacing-3;
  font-size: $font-size-sm;
  line-height: $line-height-normal;
  color: $text-primary;
  background: $bg-primary;
  border: 1px solid $border-medium;
  border-radius: 6px;
  transition: all 0.2s ease;
  
  &:focus {
    outline: none;
    border-color: $primary;
    box-shadow: 0 0 0 3px rgba($primary, 0.1);
  }
  
  &:disabled {
    background: $gray-100;
    cursor: not-allowed;
  }
  
  &.error {
    border-color: $error;
    
    &:focus {
      box-shadow: 0 0 0 3px rgba($error, 0.1);
    }
  }
}

// === Select ===
.select {
  @extend .input;
  appearance: none;
  background-image: url("data:image/svg+xml,..."); // chevron-down icon
  background-repeat: no-repeat;
  background-position: right $spacing-3 center;
  background-size: 16px;
  padding-right: $spacing-8;
}

// === Label ===
.label {
  display: block;
  margin-bottom: $spacing-2;
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  color: $text-primary;
}

// === Helper Text ===
.helper-text {
  margin-top: $spacing-1;
  font-size: $font-size-xs;
  color: $text-secondary;
  
  &.error {
    color: $error;
  }
}
```

### 表格 (Tables)

```scss
// === 表格 ===
.table {
  width: 100%;
  border-collapse: collapse;
  font-size: $font-size-sm;
  
  thead {
    background: $gray-50;
    border-bottom: 2px solid $border-medium;
  }
  
  th {
    padding: $spacing-3 $spacing-4;
    text-align: left;
    font-weight: $font-weight-semibold;
    color: $text-secondary;
    text-transform: uppercase;
    font-size: $font-size-xs;
    letter-spacing: 0.05em;
  }
  
  td {
    padding: $spacing-3 $spacing-4;
    border-bottom: 1px solid $border-light;
    color: $text-primary;
  }
  
  tbody tr {
    transition: background 0.15s ease;
    
    &:hover {
      background: $gray-50;
    }
    
    &:last-child td {
      border-bottom: none;
    }
  }
}
```

### 徽章 (Badges)

```scss
// === 徽章 ===
.badge {
  display: inline-flex;
  align-items: center;
  padding: $spacing-1 $spacing-2;
  font-size: $font-size-xs;
  font-weight: $font-weight-semibold;
  border-radius: 9999px;
  
  &-success {
    background: $success-bg;
    color: $success-dark;
  }
  
  &-warning {
    background: $warning-bg;
    color: $warning-dark;
  }
  
  &-error {
    background: $error-bg;
    color: $error-dark;
  }
  
  &-info {
    background: $info-bg;
    color: $info-dark;
  }
  
  &-gray {
    background: $gray-100;
    color: $gray-700;
  }
}
```

---

## 📐 佈局系統

### 容器 (Container)

```scss
// === 容器 ===
.container {
  width: 100%;
  margin-left: auto;
  margin-right: auto;
  padding-left: $spacing-4;
  padding-right: $spacing-4;
  
  @media (min-width: 640px) {
    max-width: $container-sm;
  }
  
  @media (min-width: 768px) {
    max-width: $container-md;
    padding-left: $spacing-6;
    padding-right: $spacing-6;
  }
  
  @media (min-width: 1024px) {
    max-width: $container-lg;
    padding-left: $spacing-8;
    padding-right: $spacing-8;
  }
  
  @media (min-width: 1280px) {
    max-width: $container-xl;
  }
}
```

### Grid 系統

```scss
// === Grid ===
.grid {
  display: grid;
  gap: $spacing-6;
  
  &-cols-1 {
    grid-template-columns: repeat(1, minmax(0, 1fr));
  }
  
  &-cols-2 {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  
  &-cols-3 {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
  
  &-cols-4 {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
  
  // 響應式
  @media (max-width: 768px) {
    &-cols-2,
    &-cols-3,
    &-cols-4 {
      grid-template-columns: repeat(1, minmax(0, 1fr));
    }
  }
  
  @media (min-width: 768px) and (max-width: 1024px) {
    &-cols-3,
    &-cols-4 {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
}
```

### Flex 工具

```scss
// === Flex ===
.flex {
  display: flex;
}

.flex-col {
  flex-direction: column;
}

.items-center {
  align-items: center;
}

.items-start {
  align-items: flex-start;
}

.items-end {
  align-items: flex-end;
}

.justify-center {
  justify-content: center;
}

.justify-between {
  justify-content: space-between;
}

.justify-start {
  justify-content: flex-start;
}

.justify-end {
  justify-content: flex-end;
}

.gap-2 {
  gap: $spacing-2;
}

.gap-4 {
  gap: $spacing-4;
}

.gap-6 {
  gap: $spacing-6;
}
```

---

## 📱 響應式設計

### 斷點定義

```scss
// === 斷點 ===
$breakpoint-sm: 640px;
$breakpoint-md: 768px;
$breakpoint-lg: 1024px;
$breakpoint-xl: 1280px;
$breakpoint-2xl: 1536px;
```

### Mixin

```scss
// === 響應式 Mixin ===
@mixin sm {
  @media (min-width: $breakpoint-sm) {
    @content;
  }
}

@mixin md {
  @media (min-width: $breakpoint-md) {
    @content;
  }
}

@mixin lg {
  @media (min-width: $breakpoint-lg) {
    @content;
  }
}

@mixin xl {
  @media (min-width: $breakpoint-xl) {
    @content;
  }
}

// === 使用範例 ===
.metric-card {
  padding: $spacing-4;
  
  @include md {
    padding: $spacing-6;
  }
  
  @include lg {
    padding: $spacing-8;
  }
}
```

---

## ✨ 動畫效果

### 動畫變數

```scss
// === 動畫時間 ===
$transition-fast: 150ms;
$transition-base: 200ms;
$transition-slow: 300ms;

// === 緩動函數 ===
$ease-in: cubic-bezier(0.4, 0, 1, 1);
$ease-out: cubic-bezier(0, 0, 0.2, 1);
$ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

### 動畫類別

```scss
// === 淡入淡出 ===
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

// === 滑入 ===
@keyframes slideIn {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

// === 脈衝 ===
@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

// === 使用範例 ===
.fade-in {
  animation: fadeIn $transition-base $ease-out;
}

.slide-in {
  animation: slideIn $transition-slow $ease-out;
}
```

---

## 📂 SCSS 架構

### 檔案結構

```
src/styles/
├── _variables.scss        # 變數定義
├── _mixins.scss          # Mixin 工具
├── _reset.scss           # CSS Reset
├── _base.scss            # 基礎樣式
├── _typography.scss      # 字體樣式
├── _utilities.scss       # 工具類別
├── components/           # 組件樣式
│   ├── _buttons.scss
│   ├── _cards.scss
│   ├── _forms.scss
│   ├── _tables.scss
│   ├── _badges.scss
│   └── _charts.scss
├── layout/               # 佈局樣式
│   ├── _container.scss
│   ├── _grid.scss
│   ├── _header.scss
│   ├── _sidebar.scss
│   └── _footer.scss
└── global.scss           # 主入口
```

### global.scss (主入口)

```scss
// src/styles/global.scss

// === 1. 變數與配置 ===
@import 'variables';
@import 'mixins';

// === 2. 重置與基礎 ===
@import 'reset';
@import 'base';
@import 'typography';

// === 3. 佈局 ===
@import 'layout/container';
@import 'layout/grid';
@import 'layout/header';
@import 'layout/sidebar';
@import 'layout/footer';

// === 4. 組件 ===
@import 'components/buttons';
@import 'components/cards';
@import 'components/forms';
@import 'components/tables';
@import 'components/badges';
@import 'components/charts';

// === 5. 工具類別 ===
@import 'utilities';
```

---

## 🎯 使用範例

### 指標卡片完整範例

```scss
// components/MetricCard.module.scss
@import '@styles/variables';
@import '@styles/mixins';

.card {
  position: relative;
  background: $bg-primary;
  border-radius: 8px;
  border: 1px solid $border-light;
  padding: $spacing-6;
  transition: all $transition-base $ease-out;
  
  &:hover {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
  }
  
  @include md {
    padding: $spacing-8;
  }
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: $spacing-4;
}

.title {
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  color: $text-secondary;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.badge {
  padding: $spacing-1 $spacing-2;
  font-size: $font-size-xs;
  font-weight: $font-weight-semibold;
  border-radius: 4px;
  
  &.good {
    background: rgba($success, 0.1);
    color: $success-dark;
  }
  
  &.needsImprovement {
    background: rgba($warning, 0.1);
    color: $warning-dark;
  }
  
  &.poor {
    background: rgba($error, 0.1);
    color: $error-dark;
  }
}

.value {
  display: flex;
  align-items: baseline;
  gap: $spacing-2;
  margin-bottom: $spacing-3;
}

.number {
  font-family: $font-family-mono;
  font-size: $font-size-3xl;
  font-weight: $font-weight-bold;
  line-height: 1;
  
  @include md {
    font-size: $font-size-4xl;
  }
  
  &.good {
    color: $success-dark;
  }
  
  &.needsImprovement {
    color: $warning-dark;
  }
  
  &.poor {
    color: $error-dark;
  }
}

.unit {
  font-size: $font-size-base;
  color: $text-tertiary;
}

.trend {
  display: flex;
  align-items: center;
  gap: $spacing-1;
  font-size: $font-size-sm;
  
  &.up {
    color: $error;
  }
  
  &.down {
    color: $success;
  }
  
  &.stable {
    color: $text-secondary;
  }
}
```

---

這就是完整的樣式規格書!需要我詳細說明某個部分或補充其他樣式嗎?